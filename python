import random

num= random.randint(1,20)

guess = int(input("can you guess the num i think ing? it is less than 20 :"))

while num!=guess:
    if guess>num:
        print("your guess is higher")
    else:
        print("your guess is lower")
    guess  = int(input("guess again:"))

print("you won!")